{
  "name": "cloudflare-secure-chat-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Signaling server for secure 1-on-1 communication via Cloudflare Workers + Durable Objects",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.95.0"
  }
}
